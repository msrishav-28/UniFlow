# Firebase Configuration Template
# Copy this file to .env.local and replace with your actual Firebase project values

# Firebase Configuration
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# Development Configuration
# Set to true to use Firebase emulators for local development
VITE_USE_FIREBASE_EMULATOR=false

# Optional: Vercel Analytics (if using)
VITE_VERCEL_ANALYTICS_ID=your_analytics_id

# How to get these values:
# 1. Go to Firebase Console (https://console.firebase.google.com/)
# 2. Select your project (or create a new one)
# 3. Go to Project Settings (gear icon)
# 4. Scroll down to "Your apps" section
# 5. Select your web app or create one
# 6. Copy the config values from the Firebase SDK snippet

# For Firebase Emulators (Development):
# 1. Install Firebase CLI: npm install -g firebase-tools
# 2. Run: firebase init emulators
# 3. Start emulators: firebase emulators:start
# 4. Set VITE_USE_FIREBASE_EMULATOR=true in your .env.local
